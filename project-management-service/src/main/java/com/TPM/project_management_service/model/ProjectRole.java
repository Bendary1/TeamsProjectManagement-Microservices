package com.TPM.project_management_service.model;

/**
 * Enum representing the roles a user can have within a project
 */
public enum ProjectRole {
    OWNER,
    ADMIN,
    MANAGER,
    DEVELOPER,
    QA,
    MEMBER
}
